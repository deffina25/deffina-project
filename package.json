{
  "name": "deffina-project",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:all": "npm run build --prefix next_deffina && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  }
}
